
import { connect } from "react-redux"
import BarcodeScannerPermissions from "./BarcodeScannerPermissions";

/*const mapStateToProps = state => {
    return {
      cameraPermissionStatus: state.camera,
      microphonePermissionStatus: state.microphone
    };
  };
export default connect(mapStateToProps)(BarcodeScannerPermissions);*/
export default BarcodeScannerPermissions
